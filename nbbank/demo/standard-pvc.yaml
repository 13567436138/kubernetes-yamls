apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: pvc-bull
  name: pvc-bull
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: glusterfs-storage-dv
  resources:
    requests:
      storage: 30Gi