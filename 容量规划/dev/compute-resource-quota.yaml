apiVersion: v1
kind: ResourceQuota
metadata:
  name: quota-dev-compute
  namespace: dev
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 4Gi
    limits.cpu: "5"
    limits.memory: 5Gi
    requests.storage: 40Gi