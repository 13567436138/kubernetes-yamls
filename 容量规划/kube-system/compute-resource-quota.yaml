apiVersion: v1
kind: ResourceQuota
metadata:
  name: quota-kube-system-compute
  namespace: kube-system
spec:
  hard:
    cpu: "4"
    memory: 10Gi
    requests.cpu: "0.1"
    requests.memory: 30Mi
    limits.cpu: "1"
    limits.memory: 2Gi