apiVersion: v1
kind: ResourceQuota
metadata:
  name: quota-kube-system-compute
  namespace: kube-system
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 4Gi
    limits.cpu: "5"
    limits.memory: 5Gi