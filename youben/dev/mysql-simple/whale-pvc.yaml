apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: pvc-whale
  name: pvc-whale
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: glusterfs-storage-dv
  resources:
    requests:
      storage: 30Gi