apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: pvc-dolphin
  name: pvc-dolphin
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: glusterfs-storage-dv
  resources:
    requests:
      storage: 30Gi