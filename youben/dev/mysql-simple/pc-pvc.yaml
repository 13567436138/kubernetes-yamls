apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: pvc-pc
  name: pvc-pc
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: glusterfs-storage-dv
  resources:
    requests:
      storage: 30Gi