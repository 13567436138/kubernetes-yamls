apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: pvc-csc
  name: pvc-csc
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: glusterfs-storage-dv
  resources:
    requests:
      storage: 30Gi