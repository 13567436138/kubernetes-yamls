apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: pvc-shop
  name: pvc-shop
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: glusterfs-storage-dv
  resources:
    requests:
      storage: 30Gi