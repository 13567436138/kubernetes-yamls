apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: pvc-universe
  name: pvc-universe
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: glusterfs-storage-dv
  resources:
    requests:
      storage: 30Gi