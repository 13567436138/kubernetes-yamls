apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: pvc-point
  name: pvc-point
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: glusterfs-storage-dv
  resources:
    requests:
      storage: 30Gi