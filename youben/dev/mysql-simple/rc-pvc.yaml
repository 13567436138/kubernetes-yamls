apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: pvc-rc
  name: pvc-rc
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: glusterfs-storage-dv
  resources:
    requests:
      storage: 30Gi