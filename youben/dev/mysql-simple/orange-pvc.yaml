apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: pvc-orange
  name: pvc-orange
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: glusterfs-storage-dv
  resources:
    requests:
      storage: 30Gi