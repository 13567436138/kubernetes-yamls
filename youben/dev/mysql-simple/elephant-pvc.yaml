apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: pvc-elephant
  name: pvc-elephant
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: glusterfs-storage-dv
  resources:
    requests:
      storage: 30Gi