apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: pvc-tiger
  name: pvc-tiger
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: glusterfs-storage-dv
  resources:
    requests:
      storage: 30Gi