apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: pvc-timor
  name: pvc-timor
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: glusterfs-storage-dv
  resources:
    requests:
      storage: 30Gi