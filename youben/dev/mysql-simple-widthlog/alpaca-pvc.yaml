apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: pvc-alpaca
  name: pvc-alpaca-withlog
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: glusterfs-storage-dv
  resources:
    requests:
      storage: 1Gi