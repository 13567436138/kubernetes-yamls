apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: shop-web
  labels:
    app: shop
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: shop
    spec:
      terminationGracePeriodSeconds: 60
      nodeSelector:
        deploy: app
      containers:
      - name: shop-web
        image: harbor.youben.demo/youben/shop-web:latest
        readinessProbe:
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 30
          timeoutSeconds: 5
        livenessProbe:
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 30
          timeoutSeconds: 5
        ports:
        - containerPort: 8080
        resources:
          limits:
            cpu: 100m
            memory: 100Mi
          requests:
            cpu: 200m
            memory: 300Mi
---
apiVersion: v1
kind: Service
metadata:
  name: shop-web
  labels:
    app: shop
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: shop
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: shop-ingress
  labels: 
    app: shop
spec:
  rules:
  - host: trade.lie100.net
    http:
      paths:
      - backend:
          serviceName: shop-web
          servicePort: 8080