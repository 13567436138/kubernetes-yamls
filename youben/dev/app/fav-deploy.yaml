apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: fav-web
  labels:
    app: fav
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: fav
    spec:
      terminationGracePeriodSeconds: 60
      nodeSelector:
        deploy: app
      containers:
      - name: fav-web
        image: harbor.youben.demo/youben/fav-web:latest
        readinessProbe:
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 30
          timeoutSeconds: 5
        livenessProbe:
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 30
          timeoutSeconds: 5
        ports:
        - containerPort: 8080
        resources:
          limits:
            cpu: 100m
            memory: 100Mi
          requests:
            cpu: 200m
            memory: 300Mi
---
apiVersion: v1
kind: Service
metadata:
  name: fav-web
  labels:
    app: fav
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: fav
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: fav-ingress
  labels: 
    app: fav
spec:
  rules:
  - host: fav.lie100.net
    http:
      paths:
      - backend:
          serviceName: fav-web
          servicePort: 8080