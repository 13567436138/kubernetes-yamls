apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: openapi-web
  labels:
    app: openapi
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: openapi
    spec:
      terminationGracePeriodSeconds: 60
      nodeSelector:
        deploy: app
      containers:
      - name: openapi-web
        image: harbor.youben.demo/youben/openapi-web:latest
        readinessProbe:
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 30
          timeoutSeconds: 5
        livenessProbe:
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 30
          timeoutSeconds: 5
        ports:
        - containerPort: 8080
        resources:
          limits:
            cpu: 100m
            memory: 100Mi
          requests:
            cpu: 200m
            memory: 300Mi
---
apiVersion: v1
kind: Service
metadata:
  name: openapi-web
  labels:
    app: openapi
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: openapi
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: openapi-ingress
  labels: 
    app: openapi
spec:
  rules:
  - host: ybapi.lie100.net
    http:
      paths:
      - backend:
          serviceName: openapi-web
          servicePort: 8080