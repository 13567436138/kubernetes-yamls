apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: whale-web
  labels:
    app: whale
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: whale
    spec:
      terminationGracePeriodSeconds: 60
      nodeSelector:
        deploy: app
      containers:
      - name: whale-web
        image: harbor.youben.demo/youben/whale-web:latest
        readinessProbe:
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 30
          timeoutSeconds: 5
        livenessProbe:
          httpGet:
            path: /
            port: 8080
            scheme: HTTP
          initialDelaySeconds: 30
          timeoutSeconds: 5
        ports:
        - containerPort: 8080
        resources:
          limits:
            cpu: 100m
            memory: 100Mi
          requests:
            cpu: 200m
            memory: 300Mi
---
apiVersion: v1
kind: Service
metadata:
  name: whale-web
  labels:
    app: whale
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: whale
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: whale-ingress
  labels: 
    app: whale
spec:
  rules:
  - host: salary.ybdata.net
    http:
      paths:
      - backend:
          serviceName: whale-web
          servicePort: 8080